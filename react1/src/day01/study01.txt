-- 07/13
리액트 실행하기 + 확장 프로그램 다운
크롬 웹스토어에서 React Developer Tools 다운(관리자 모드에 추가 버튼 생김 컴포넌츠, 프로파일러)

vsCode에서 쓸 플러그인 : React Extension Pack, ES7+ React/Redux/React-Native snippets


리액트 구성편짐 명령어 run, 스크립트 start

구성편집, javascript 디버그 url : localhost:3000

인텔리제이 새프로젝트 리액트 위치설정, npx create-react-app 외 기본값

리액트 프로젝트 구성

node_modules : node.js 모듈이 설치되는 폴더, 리액트 프로젝트가 동작되는데 필요한 모듈이 설치되는 폴더
                build 시 필요없는 부분은 자동 제외됨

public : 리액트 프로젝트의 기본 파일이 위치하는 곳(스프링의 static과 유사), index.html 파일에 있는 div 태그를 조작하여 리액트 프로그램이 조작하여 화면을 표시함

src : 리액트 프로젝트의 소스코드가 존재하는 폴더

index.js : 리액트 프로젝트의 시작점,리액트 프로젝트 시작 시 index.html 파일 안의 div 태그 중 id값이 'root'인 태그를 검색하여 선택하고 index.js를 로딩하여 index.js의 내용을 실행하여 화면을 구성

MPA : Multi Page Application 의 줄임말, 웹 페이지가 여러개 있는 형태의 웹 사이트, 기존 방식의 웹사이트(이때까지 만들어 오던 방식 spring 포함, 리소스 폴더 안에 여러개 웹 페이지 있음)
SPA : Single Page Application 의 줄임말, 웹 페이지가 단 하나로 이루어진 웹 사이트, 자바스크립트를 이용하여 필요에 따라서 웹페이지를 새로 그려내는 사이트(Page<content1(component 동의어), content2, ... 교체하여 사용)

App.js : 컴포넌트

Virtual DOM(가상돔) : 리액트에서 웹 페이지를 그려내기 위해서 사용하는 방식, (수정이 발생하면 가상의 돔에서 새로 그려주고 다 그려진걸 실제의 돔에 바로 보여줌.)
- 기본적으로 웹은 화면을 한번 출력하고 다시 수정된 화면을 출력 시 모든 화면을 다시 그려야 함
- 화면을 재출력하기 위해서 새로 그리면 화면 깜빡임 현상이 발생함
- 조금의 수정이 발생해도 모든 리소스를 다시 호출하기 때문에 리소스 성능이 떨어짐
- 리액트는 virtual DOM을 사용하여 실제 화면은 그대로 두고 가상돔의 내용만 먼저 수정하고 수정이 끝난 후 가상돔의 화면과 실제 화면을 교체하여 화면이 빠르게 로딩되는 것처럼 만들어 줌

리액트는 컴포넌트 기반으로 화면을 구성함.
- 컴포넌트는 화면을 구성하는 부분부분을 뜻함
- JSP나 thymeleaf를 이용하여 여러가지 파일을 하나로 합하여 하나의 웹페이지를 구성하는 것과 비슷함
- 재사용성이 높아짐.

JSX : 리액트에서 사용하는 자바스크립트 확장 문법(문법 기억하기)
- Javascript + XML/HTML 을 의미함
- 자바스크립트 변수에 HTML 태그를 그대로 사용하여 화면을 구성함
- xml 문법 규칙을 기본으로 하기 때문에 시작 태그와 끝 태그가 반드시 한쌍으로 구성되야 함(<br></br> 또는 <br/> 이런 식으로 html 에선 닫는 태그가 필요 없는 태그들도 반드시 닫는 태그를 사용하여야 한다)
- 시작 태그의 마지막에 끝 태그를 뜻하는 / 를 추가하면 시작 태그만 있어도 상관없음
    - <br></br>
    - <hr/>
- render() 메소드의 return 에 화면을 그리기 위한 html 태그 및 컴포넌트를 입력하여 화면을 구성함
- render() 메소드의 return 에 들어가는 태그가 1개 일 경우 ()가 필요 없음
- render() 메소드의 return 에 들어가는 태그가 2개 이상 일 경우 반드시 ()로 감싸야함(중요!! 필수 규칙)
- render() 메소드의 return 에 들어가는 태그는 부모 태그는 반드시 1개 이어야 함


-- 07/14
element (엘리먼트) : 리액트 앱의 가장 작은 빌드 블록
- 웹 페이지의 정보를 담고 있는 DOM 에서 사용하는 용어 (HTML 태그를 뜻함)
- 리액트에서는 엘리먼트를 리액트 엘리먼트라고 함
- 리액트는 Virtual DOM을 사용하고 리액트 엘리먼트는 Virtual DOM에 존재함
- 리액트 엘리먼트는 DOM 엘리먼트의 가상 표현(리액트는 가상 돔을 사용하므로)
- 리액트에서 엘리먼트는 자바스크립트의 object 타입으로 존재함
- createElement() 함수가 아래와 같이 생성함

엘리먼트의 특징 : 엘리먼트는 생성 후에는 children 이나 attributes를 변경할 수 없음(불변성)
- 엘리먼트는 생성 후 변경할 수 없기 때문에 수정된 내용이 필요 시 새로 생성하여 바꿔치기함.
- 이를 위해서 Virtual DOM을 사용함

컴포넌트 : 리액트에서 화면을 구성하는 자바스크립트 클래스 혹은 함수
- 여러개의 컴포넌트를 레고 블럭처럼 사용하여 하나의 큰 화면을 구성함
- 개념적으로 자바스크립트 함수와 비슷함
- props를 입력받아 화면을 구성하고 엘리먼트를 출력함
- 자바의 클래스와 객체의 관계와 비슷함

props: 리액트 컴포넌트의 속성
- 자바스크립트 object 타입으로 구성되어 있음
- 읽기 전용
- 데이터 전달 :
    - 부모 컴포넌트에서 자식 컴포넌트 호출 시 전달할 속성명과 값을 함께 전달
    - <Button 속성명1=속성값1, 속성값2 = 속성값2, ...></Button>
    - 문자열 데이터는 '', ""를 사용하여 데이터 전달(문자열도 {} 사용은 가능)
    - 문자열을 제외한 다른 데이터 타입은 반드시 {}를 사용하여 전달
    - 데이터를 사용하는 자식 컴포넌트에서는 'props.속성명' 형태로 사용함